{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path // empty' | { read -r f; case \"$f\" in \"$PWD\"/*) node node_modules/prettier/bin/prettier.cjs --write --ignore-unknown \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting"
          }
        ]
      }
    ]
  }
}
