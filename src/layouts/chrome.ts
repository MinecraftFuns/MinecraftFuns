/**
 * How much of the site a page wears. `site` is brand, nav, and footer;
 * `document` keeps the footer's attribution and drops the bar above, which the
 * docs do: reference material is not part of the first-person voice the nav
 * frames everything else in.
 */
export type Chrome = "site" | "document";
